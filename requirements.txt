# 核心依赖（脚手架阶段即可用）
python-dotenv>=1.0.0

# D12 向量检索：retriever 惰性 import，缺失时自动降级为关键词打分
numpy>=1.26.0
# chromadb>=0.5.0   # 知识库上千条目后再启用（当前 numpy 暴力余弦足够，零服务依赖）

# D14 精确代码解析阶段启用（比内置 ast 支持 Java/C++）
# tree-sitter>=0.23.0
# tree-sitter-languages>=1.10.0

# 报告 HTML 渲染（D16；缺失时自动降级为纯文本）
markdown>=3.5

# 测试
pytest>=8.0.0

# 桌面应用形态（F 项，可选）：python desktop.py
fastapi>=0.110
uvicorn>=0.29
pywebview>=5.0    # 装不上会自动退化为浏览器模式，功能一致
